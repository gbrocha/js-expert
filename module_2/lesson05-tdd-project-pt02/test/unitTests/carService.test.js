const { describe, it } = require('mocha')
const CarService = require('../../src/service/carService')

describe('CarService Suite Tests', () => {
  it('give a carCategory it should return an available car', () => {})
})
